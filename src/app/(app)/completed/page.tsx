import { TaskView } from '@/features/tasks/TaskView';

export default function CompletedPage() {
  return <TaskView view="completed" title="Completed" subtitle="Your finished work, kept in view." />;
}
