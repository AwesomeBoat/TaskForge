import { TaskView } from '@/features/tasks/TaskView';

export default function UpcomingPage() {
  return <TaskView view="upcoming" title="Upcoming" subtitle="A little visibility for what is next." />;
}
